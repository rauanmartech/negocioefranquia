import MediaLibrary from "@/components/admin/MediaLibrary";

export const metadata = {
  title: "Biblioteca de Mídia — NEF Admin",
};

export default function MediaPage() {
  return <MediaLibrary />;
}
